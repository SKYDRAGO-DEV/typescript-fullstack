{
  "name": "typescript-fullstack",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/api"],
  "scripts": {
    "dev": "npm run dev --workspace=@skydrago/api",
    "build": "npm run build --workspace=@skydrago/api",
    "typecheck": "npm run typecheck --workspace=@skydrago/api",
    "check": "npm run typecheck && npm run build"
  }
}
